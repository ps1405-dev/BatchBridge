'use client';
import {MapContainer,TileLayer,CircleMarker,Popup} from 'react-leaflet';
import 'leaflet/dist/leaflet.css';
export default function NearbyMap({maker,leads}){const points=leads.filter(x=>Number.isFinite(Number(x.latitude))&&Number.isFinite(Number(x.longitude)));const center=maker.latitude?[Number(maker.latitude),Number(maker.longitude)]:points.length?[Number(points[0].latitude),Number(points[0].longitude)]:[20.5937,78.9629];return <div style={{height:430,borderRadius:14,overflow:'hidden',border:'1px solid #d8d4c9',margin:'28px 0'}}><MapContainer center={center} zoom={maker.latitude?14:5} style={{height:'100%',width:'100%'}}><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{maker.latitude&&<CircleMarker center={[maker.latitude,maker.longitude]} radius={11} pathOptions={{color:'#ef8952',fillColor:'#ef8952',fillOpacity:1}}><Popup>Your maker location</Popup></CircleMarker>}{points.map(l=><CircleMarker key={l.id} center={[l.latitude,l.longitude]} radius={Math.max(6,Math.min(12,Number(l.score||0)/10))} pathOptions={{color:'#176b4f',fillColor:'#1d8c68',fillOpacity:.8}}><Popup><b>{l.business_name}</b><br/>AI fit: {l.score}/100</Popup></CircleMarker>)}</MapContainer></div>}
